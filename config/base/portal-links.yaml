# doc 12 §32/§76/§128. Phase 23 populated config/portals/affiliation.yaml with
# placeholder Club Portal hostnames (TODO: replace with the real hostname once
# Enterprise Integration confirms it) — listed here so link resolution can actually
# succeed against them; every other portal/domain still fails closed
# (DOMAIN_NOT_ALLOWLISTED) until it's likewise approved and added.
link_policy:
  max_links_per_response: 10
  allowed_domains:
    - portal-dev.pff.example
    - portal-test.pff.example
    - portal-uat.pff.example
    - portal-staging.pff.example
    - portal.pff.example
